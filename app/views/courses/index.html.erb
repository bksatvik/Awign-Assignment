<h1>Courses Index</h1>

<% @courses.each do |course| %>
    <h3><%=course.name %></h3>
    <%= button_to "Delete Course", course_path(course), method: :delete %>
    <br>
<% end%>

<%= link_to 'Create New Course', '/courses/new' %>
<br>
<%= link_to 'Back to All Students', students_path %>